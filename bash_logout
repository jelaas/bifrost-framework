#!/bin/sh
#
# name: .bash_logout
#
# Remounting the system disk read-only if mounted read-write
#
#REMOUNT=`cat /proc/mounts|grep /dev/root|grep rw`
#if [ "$REMOUNT." != "." ]; then
#         remount r
#fi
